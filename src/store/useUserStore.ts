// src/store/useUserStore.ts  — canonical store path per required structure.
// The implementation lives in useStore.ts; re-exported here under the new name.
export { useStore as useUserStore } from './useStore';
